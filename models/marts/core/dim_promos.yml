 version: 2

models:
  - name: dim_promos
    description: ""
    columns:
      - name: promo_id
        description: "PK de la tabla, identifica de manera única cada promo"

      - name: discount_usd
        description: "descuento que se aplica al order"

      - name: status
        description: "estado de la promo"

      - name: sync_date
        description: "fecha de sincronización del registro"

      - name: sync_time
        description: "hora de sincronización del registro"